services:
    facebook_messenger.messenger:
        class: Kerox\Messenger\Messenger
        arguments:
            $appSecret:   %facebook_messenger.app_secret%,
            $verifyToken: %facebook_messenger.verify_token%
            $pageToken:   %facebook_messenger.page_token%
            $apiVersion:  %facebook_messenger.api_version%